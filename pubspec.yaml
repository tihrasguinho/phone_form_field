name: phone_form_field
description: "A new Flutter package project."
version: 0.0.4+1
homepage: https://gitlab.com/softbuilder/phone_form_field

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  diacritic: ^0.1.5
  extended_masked_text: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
